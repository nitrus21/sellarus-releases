# Sellarus stack settings (written by install.sh). Keep this file private: it holds the database passwords.
DOMAIN=shop.example.com
ACME_EMAIL=you@example.com
DB_NAME=sellarus
DB_USER=sellarus
DB_PASSWORD=change-me
DB_ROOT_PASSWORD=change-me-too
DB_PREFIX=vb_
SELLARUS_TAG=latest
