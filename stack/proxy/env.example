# Settings of the shared HTTPS front (written by install.sh into /opt/sellarus/proxy/.env).
ACME_EMAIL=you@example.com
# Ports opened on the server (change only if another web server must keep 80/443).
HTTP_PORT=80
HTTPS_PORT=443
