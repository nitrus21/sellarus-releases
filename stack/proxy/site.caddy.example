# Example of sites/<shop>.caddy — generated by install.sh, rewritten by `sellarus domain <shop> <domain>`.
# A shop without a domain uses ":80" instead of the domain (HTTP only, one such shop per server).
shop.example.com {
	encode zstd gzip
	reverse_proxy sellarus-boutique:80
}
